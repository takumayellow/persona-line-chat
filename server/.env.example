OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
PORT=3001

# 1回の返信に使わせるトークンの上限（未設定・0以下・非数値なら 400）
MAX_COMPLETION_TOKENS=400

# 別オリジンから叩かせたいときだけカンマ区切りで並べる。
# 未設定＝クロスオリジンを一切許さない。本物のクライアントは開発も本番も同一オリジンなので、
# 通常は空のままでよい。
ALLOWED_ORIGINS=
